{
  "name": "wikivendas-portal",
  "version": "1.0.0",
  "description": "Portal dinâmico Wikivendas alimentado pelo Notion",
  "scripts": {
    "build": "node scripts/fetch-notion.js && node scripts/generate-site.js",
    "dev": "node scripts/fetch-notion.js && node scripts/generate-site.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "dotenv": "^16.4.7",
    "marked": "^15.0.4"
  }
}
